CREATE TABLE government_users (
  ID	int,
  FIRST_NAME	varchar(80),
  LAST_NAME	varchar(80),
  PHONE	varchar(80),
  ADDRESS	varchar(80),
  BIRTHDATE	varchar(80)
);

CREATE VIEW dejima_bank (
  ID	int,
  FIRST_NAME	varchar(80),
  LAST_NAME	varchar(80),
  PHONE	varchar(80),
  ADDRESS	varchar(80)
);
